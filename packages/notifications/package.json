{
  "name": "@lowroute/notifications",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --pretty false --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@lowroute/config": "workspace:*",
    "@lowroute/domain": "workspace:*"
  }
}
